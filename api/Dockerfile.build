FROM golang:1.26

RUN apt-get update && apt-get install -y --no-install-recommends \
    gcc libc6-dev libwebp-dev \
    && rm -rf /var/lib/apt/lists/*

WORKDIR /app
COPY go.mod go.sum ./
RUN go mod download

COPY . .
RUN CGO_ENABLED=1 go build -o utterlog-api .
